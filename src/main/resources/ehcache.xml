<config xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance' xmlns='http://www.ehcache.org/v3' xmlns:jsr107="http://www.ehcache.org/v3/jsr107" xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core.xsd http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.0.xsd">
    <service> <jsr107:defaults enable-management="true" enable-statistics="true"/> </service>
    <cache alias="categoryCache">
        <expiry> <!-- 캐시 만료 시간 = timeToLiveSeconds -->
            <ttl unit="minutes">60</ttl>
        </expiry>
        <resources> <!-- JVM heap 메모리, LRU strategy-->
            <heap unit="entries">1000</heap>
        </resources>
    </cache>
    <cache alias="categoryChildCache">
        <expiry> <!-- 캐시 만료 시간 = timeToLiveSeconds -->
            <ttl unit="minutes">60</ttl>
        </expiry>
        <resources> <!-- JVM heap 메모리, LRU strategy-->
            <heap unit="entries">1000</heap>
        </resources>
    </cache>
    <cache alias="categoryDetailCache">
        <expiry> <!-- 캐시 만료 시간 = timeToLiveSeconds -->
            <ttl unit="minutes">60</ttl>
        </expiry>
        <resources> <!-- JVM heap 메모리, LRU strategy-->
            <heap unit="entries">1000</heap>
        </resources>
    </cache>
    <cache alias="categoryDetailItemCache">
        <expiry> <!-- 캐시 만료 시간 = timeToLiveSeconds -->
            <ttl unit="minutes">60</ttl>
        </expiry>
        <resources> <!-- JVM heap 메모리, LRU strategy-->
            <heap unit="entries">1000</heap>
        </resources>
    </cache>
</config>
